<div class="container">
    <div class="row mt-4">
        <div class="col-3 border">
            <button class="btn btn-primary">Left Layout</button>
        </div>
        <div class="col-9 border">
            <h2>{{course.name}}</h2>
            <iframe width="100%" height="315" src="https://www.youtube.com/embed/{{course.videoId}}">
            </iframe>
            <p>{{course.description}}</p>
        </div>
    </div>
</div>
